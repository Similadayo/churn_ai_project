# Pinned to the exact versions the project was built and tested with.
# These MUST stay pinned: the saved model (models/churn_model.pkl) was created
# with scikit-learn 1.4.2 / numpy 1.26.4 and will fail to load on newer versions.
# Tested on Python 3.11.

pandas==2.1.4
numpy==1.26.4
scikit-learn==1.4.2
imbalanced-learn==0.14.0
matplotlib==3.7.5
seaborn==0.13.2
streamlit==1.51.0
joblib==1.3.2
shap==0.49.1
plotly==6.3.1
altair==5.5.0
Pillow==12.0.0
